# data

- `news_events.csv` — `datetime_utc, currency, tier (1–3), title`. Экспорт high-impact ForexFactory за 6 месяцев (открытый вопрос №3).
- `tags_{symbol}.csv`, `LION_overlay_{symbol}.csv` — выходы таггера, в .gitignore (большие, генерируются бэктестером).
