namespace LionRegime.Core.Liquidity;

/// <summary>
/// Чем пул сняли. Различие принципиально: открытый research-вопрос проекта (случай 11/06, +26.8R)
/// в том, что текущее правило входа принимает только WICK, а BODY-свипы не проверены.
/// Разметка обязана логировать оба типа отдельно.
/// </summary>
public enum SweepType
{
    /// <summary>Бар вышел за уровень, но закрылся обратно за ним. Классический сбор стопов.</summary>
    WICK,

    /// <summary>Бар закрылся ЗА уровнем. Может быть пробоем, а может быть свипом с отложенным возвратом.</summary>
    BODY,
}
